<html>
<head>
<h1 style="text-align: center">List of Contact</h1>

</head>

<body>

<div>
<form method="GET" action="/contact/search">

<label for="search">Search :</label>
<input type="search" id="search" name="search"
       aria-label="Search by FullName" />

<input type="submit" >Search</input>
</div>
 </form>       
<table border="1">
                <tr>
                        <td>nom</td>
                        <td>phone</td>
                        <td>action</td>
                </tr>


        {% for item in data %}
                <tr>
                        <td>{{item.FullName}}</td>
                        <td>{{ item.Phone }}</td>
                        <td><a href="/contact/delete/{{item._id}}">Delete</a></td>
                        <td><a href="/contact/update/{{item._id}}">Update  </a></td>
                        {# <td><a href="/contact/updatee/{{item._id}}">Update 2 </a></td> #}
                </tr>

        {% endfor %}
</table>
</body>

</html>